# TODO: Use the Anthropic API to re-rank or bonus-score top listings.
# Will send listing title + description to Claude and ask for a gem assessment.


def ai_bonus_score(listing: dict) -> int:
    # Not yet implemented — returns 0 (no effect on ranking)
    return 0
